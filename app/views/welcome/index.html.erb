<% if current_user %>

  <br/>
  <br/>
  <br/>
<% if current_user.under_13? %>

<div class="row">
  <div class="col-lg-2">
  </div>
  <div class="col-lg-3" style="text-align: center; font-size: 24px;">
    Welcome back <%= current_user.fname %>
  </div>
  <div class="col-lg-4" style="text-align: center; font-size: 18px;">
    You are at Level <%= current_user.level %>
  <br/>
  Click <%= link_to 'here', bump_level_kid_path(current_user), method: 'post' %> to goto next level.
  </div>
</div>
<br/>
<br/>
<br/>


  <table class="table">
  <% current_user.accounts.each do |a| %>
    <tr>
      <td> <%= link_to a.flavor, account_activities_path(a) %></td>
      <td></td>
      <td></td>
      <td> <%= link_to number_to_currency(a.balance, unit: 'ϟ'), account_activities_path(a) %></td>
      </tr>
  <% a.last_few.each do |a| %>
      <tr>
        <td></td>
        <td><%=time_ago_in_words(a.happened_at) %> ago</td>
        <td><%=a.action %></td>
        <td><%=number_to_currency(a.amount, unit: '') %></td>
      </tr>
  <% end %>
      <tr>
    </tr>
  <% end %>
  </table>
<% elsif current_user.under_18? %>
  teenagers!
<% else %>
  <table class="table">
    <tr>
      <th>Level</th>
      <th>Age For Level</th>
      <th>Kid</th>
      <th>Co-Parent</th>
      <th>Observers</th>
    </tr>
  <% current_user.kids.compact.each do |k| %>
    <tr class="black">
      <td> Level <%=k.level%>, <%= k.level_text %></td>
      <td> <%= k.age %> &#x2714; </td>
      <td> 
        <div><%= k.name %> <%= link_to 'x', kid_path(k), method: 'delete', data: {confirm: 'u sure?'}%>
        </div>
        <div>
          Password: <code><%=k.password%></code> <%= link_to 'login as', login_as_kid_path(k), method: 'post' %>
        </div>
    </td>
      <td> <a href="#" onclick="$('#set-co-form<%=k.id%>').show(); return false;"><%= k.co_parent %></a>
      
  <%= form_for(Customer.new, url: set_co_parent_kid_path(k), method: 'post', html: {id: 'set-co-form'+k.id.to_s, style: 'display: none;'}) do |f| %>
    <div class="field">
      <%= f.label 'Name (first & last)' %><br>
      <%= f.text_field :fname, id: 'fname'%>
    </div>
    <div class="field">
      <br/>
      <%= f.label 'Email' %><br>
      <%= f.text_field :email %>
    </div>
    <div class="actions white">
      <br/>
      <%= f.submit value: 'Set as Co-Parent', class: 'btn btn-info' %>
      <a href="#" style="color: white;" onclick="$('#set-co-form<%=k.id%>').hide(); return false;" class="btn btn-danger">Cancel</a>
    </div>
  <% end %>
      
      </td>
      <td> <a style="color: black; text-decoration: none;" href="#" onclick="$('#set-o-form<%=k.id%>').show(); return false;"><%= k.non_co_parent_observers.first || 'none set yet' %></a></td>
    </tr>
  <% end %>
  </table>
  <%= form_for(@kid, html: {id: 'add-kid-form', style: 'display: none;'}) do |f| %>
    <div class="field">
      <%= f.label 'First Name' %><br>
      <%= f.text_field :fname, id: 'fname'%>
    </div>
    <div class="field">
      <br/>
      <%= f.label 'Last Name' %><br>
      <%= f.text_field :lname %>
    </div>
    <div class="field">
      <br/>
      <%= f.label 'Born on' %><br>
      <%= f.date_select :dob, {start_year: 4.years.ago.year-14, end_year: 4.years.ago.year},
        style: 'font-size: 24px;' %>
    </div>
    <div class="actions">
      <br/>
      <%= f.submit value: 'Add This Kid', class: 'btn btn-info' %>
      <a href="#" onclick="$('#add-kid-form').hide()" class="btn btn-danger">Cancel</a>
    </div>
  <% end %>
<% end %>

<% else %>
<br/>
<br/>
<br/>
<div class="row">
  <div class="col-lg-2">
  </div>
  <div class="col-lg-5" style="text-align: center;">
<p style="font-size: 24px;">
Are you a kid age 4 through 12?
</p>
<p style="font-size: 18px;">
Finally a bank for you! With a real ATM Card that works at any KidBank branch.
Ask your parents if it's okay for you to open a KidBank account today!
</p>

  <br/>
  <br/>
<p style="font-size: 18px;">
Already have an account? <a class="btn btn-danger" href="/sessions/new">Login here</a>
</p>
  </div>
  <div class="col-lg-2" style="text-align: center;">
  </div>
  <div class="col-lg-2" style="text-align: center;">
  </div>
</div>
<br/>
<br/>
<br/>

  <br/>
  <br/>
  <br/>
  <br/>
<% end %>
